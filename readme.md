# Client

- `cd client`
- `yarn install`
- `yarn dev`
- Client can be accessed http://localhost:3000

# Server

- `cd server`
- `yarn install`
- `yarn dev`
- Client can be accessed http://localhost:4000
